// Brand-named alias for the design-system Button. Use this in feature
// screens so the spec language ("LuxuryButton") maps directly to a component.
export { Button as LuxuryButton } from './Button';
